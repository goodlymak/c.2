/* 
Author:Mukadam Umar Ahmed.
Date: 30-09-2022.
program:find greater number for 3 numbers
*/
#include <stdio.h>

int main()
{
    int a;
    int b ;
    int c ;
    printf("enetr number:a= ");
    scanf("%d",&a);
    printf("enetr number:b= ");
    scanf("%d",&b);
    printf("enetr number:c= ");
    scanf("%d",&c);
        if (a > b)
        {
        printf("a is greater" );
        }
            else if (b > c)
            {
            printf("b is greater");
            }
            else 
            {
            printf("c is greater");
            }
            return 0;
    }
    
    
    
    Output:
 enetr number:a= 20
enetr number:b= 40
enetr number:c= 70
c is greate
