/*
Author:mukadam umar ahmed
date:
program:Conditional Operator ( ? : )
Conditional operators return first value if condition is true and returns another value is condition is false.
This operator is also called as a ternary operator as it takes three arguments.
Syntax : (Condition ? True value : false value ); 
*/
#include <stdio.h>

void main() 
    {
        int i;
        printf("enter a number= ");
        scanf("%d", &i);
        i%2==0?printf("%d number is even",i): printf("%d number is odd",i);
    
    }

output:
enter a number= 2
2 number is even
